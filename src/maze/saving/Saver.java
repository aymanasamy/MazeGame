package maze.saving;

import maze.Maze;
import maze.builders.Difficulty;

public class Saver {
    void save(Difficulty difficulty,int level,Maze maze) {
        // Todo
    }
    Maze load(Difficulty difficulty,int level) {
        // Todo
        return null;
    }
    boolean isSaved(Difficulty difficulty,int level){
        // Todo
        return false;
    }
}
